const monsterSearchResult = (props) => (
	<div className="results__result">
		<h3>{props.name}</h3>
		<p>{props.alignment} {props.type}</p>
	</div>
	)

const monsterSearchResults = () => (
	<div className="results">
		
	</div>
	)